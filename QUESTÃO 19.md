# Logica2
Questões de logica
int main()
{
    int a,b,x,r;
    scanf("%d",&a);
    scanf("%d",&b);
    
    x = -b / a;
    r = sqrt (x);
    
    printf("Raiz quadrada %d", r);
    
    return 0;
}
